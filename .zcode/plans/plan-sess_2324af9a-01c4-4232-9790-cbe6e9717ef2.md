## 优化升级实施计划

按照《最终交付实施操作手册》的7个步骤，对项目进行完整优化升级：

### 第1步：修复MySQL Schema
- 修改 `sql/mysql/001_schema.sql`：dws_station_monthly增加5个天气字段（heat_wave_days等），所有ADS表主键改为支持多年数据的复合主键
- 创建3个拆分SQL脚本：`001_init.sql`（空库建表）、`002_add_dws_weather_days.sql`（增量ALTER）、`003_ads_year_keys.sql`（重建ADS主键）

### 第2步：更新数据校验脚本
- `scripts/verify_data.py`：硬编码密码改环境变量、旧表名`dws_monthly`→`dws_station_monthly`、固定阈值改动态计算、增加DWS/ADS完整性检查

### 第3步：收口后端配置和API
- `.env.example`：MYSQL_USER改为climate_app，增加CORS端口
- `.gitignore`：增加frontend/.env
- `backend/config.py`：添加dotenv加载
- `backend/requirements.txt`：增加gunicorn
- `backend/app.py`：修复static_url_path和SPA fallback
- Agent路由/service：增加年份整数校验

### 第4步：Agent安全加固
- 补充年份参数校验、LLM_MODEL配置读取

### 第5步：Docker化部署
- 创建 `docker-compose.yml`（mysql+backend+frontend三服务）
- 创建 `backend/Dockerfile`、`frontend/Dockerfile`、`frontend/nginx.conf`
- 创建 `.dockerignore` 文件

### 第6步：前端收尾
- 修改 `index.html` title
- CityRanking.vue、ClimateZones.vue 增加loading/error/空状态、单位显示、年份标注

### 第7步：测试和文档
- 创建4个测试文件覆盖意图识别、Agent安全、API、ETL质量
- 更新README.md，创建部署运行手册、API文档、数据字典

严格按1→7顺序执行，预计修改20+文件。